#include <stdio.h>

int main(void)
    for int e = 0; e <= 2000; e++ do
        for int a = 1; a <= e; a++ do
            int x = e / a
            if x * a == e do
                int b = x - a; // x = a + b
                if b >= 50 && b < 100 do
                    int c = x + a; //  x = c - a
                    if c >= 0 && c <= 2000 do
                        int d = x / a; // x = a * d
                        if d * a == x do
                            if d >= 0 && d <= 2000 do
                                if b + c + d + e == 2000 do
                                    printf("%d, %d, %d, %d, %d; %d\n", a, b, c, d, e, x)
    return 0
